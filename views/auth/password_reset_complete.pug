extends _layout

block vars
  include ../config
  - var key = "password_reset_complete"
  - var page = pages[key]

block content
  main
    .wrapper
      .auth-section(class="uk-width-3-5@s uk-width-2-5@m uk-width-1-3@l uk-padding")
        .auth-header
          h1.site-logo(class="uk-text-center") #{site_title}
          p.page-title(class="uk-text-center") #{page.title}
          h2.auth-text-light-grey(class="uk-text-center") パスワードリセットメール送信完了
          p.auth-text-grey
            | パスワード再設定用のURLを記載したメールをお送りしました。
            | 内容に従って、新しいパスワードを設定してください。<br>
            | しばらくしてもメールが届かない場合は、迷惑メールフォルダをご確認ください。<br>
